{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ED wait observation",
  "$id": "https://mem-ed-wait-times.example/schemas/ed-wait.schema.json",
  "description": "One facility/metric observation in a collection batch. Applies to raw and decompressed compacted JSONL and latest.json last_success records. Six-field provenance remains unchanged. Historical samples checked on 2026-09-11; M0 validation and integer conversion tightened on 2026-09-14 and deployed on 2026-09-22. See s3-buckets.md and aws-deployment-2026-09-22.md for dated production verification.",
  "$comment": "This schema describes the current producer's six-field record, unchanged by M1. M1 derived baseline models and historical display tuples have a separate comparisons.schema.json contract; they are not raw observations. This schema does not validate complete JSONL files, enforce uniqueness, or establish coverage. Facility and metric values remain open. Enable date-time format checking.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "facility",
    "metric",
    "wait_minutes",
    "observed_at",
    "batch_id",
    "latency_ms"
  ],
  "properties": {
    "facility": {
      "type": "string",
      "minLength": 1,
      "description": "Facility slug supplied to the upstream API as the fac parameter. Current configured values are listed in s3-buckets.md.",
      "examples": ["arlington", "memphis", "childrens"]
    },
    "metric": {
      "type": "string",
      "minLength": 1,
      "description": "Metric key from the upstream API response. All inspected records use CV_ED_Wait, but the producer accepts other keys. The repository does not define its precise clinical meaning.",
      "examples": ["CV_ED_Wait"]
    },
    "wait_minutes": {
      "type": "integer",
      "description": "Upstream integer or integer string converted to an integer in minutes. M0 rejects booleans, decimals and null instead of truncating them. No bounds or sentinel meanings are established for waits. Zero is retained. Existing stored values are not rewritten.",
      "examples": [0, 34, 122]
    },
    "observed_at": {
      "type": "string",
      "format": "date-time",
      "description": "Observation timestamp captured after the facility HTTP response succeeds. The producer writes ISO 8601 UTC with a +00:00 offset and fractional seconds when present. This is collection time, not an upstream event timestamp.",
      "examples": ["2026-09-11T00:08:51.380937+00:00"]
    },
    "batch_id": {
      "type": "string",
      "format": "date-time",
      "description": "Collection start timestamp shared by all records in a batch, written as ISO 8601 UTC with a +00:00 offset. Its UTC date determines the dt partition; its time at second precision determines the raw object filename. This is not a UUID.",
      "examples": ["2026-09-11T00:08:50.867368+00:00"]
    },
    "latency_ms": {
      "type": "integer",
      "minimum": 0,
      "description": "Python Requests response.elapsed.total_seconds() multiplied by 1000 and rounded, in milliseconds. This measures the facility HTTP request through response headers, not the complete collection batch or a patient wait.",
      "examples": [512]
    }
  },
  "examples": [
    {
      "facility": "arlington",
      "metric": "CV_ED_Wait",
      "wait_minutes": 34,
      "observed_at": "2026-09-11T00:08:51.380937+00:00",
      "batch_id": "2026-09-11T00:08:50.867368+00:00",
      "latency_ms": 512
    }
  ]
}
